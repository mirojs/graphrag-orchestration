{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "neo4jPassword": {
      "value": "${NEO4J_PASSWORD}"
    },
    "voyageApiKey": {
      "value": "${VOYAGE_API_KEY}"
    },
    "mistralApiKey": {
      "value": "${MISTRAL_API_KEY}"
    },
    "llmwhispererApiKey": {
      "value": "${LLMWHISPERER_API_KEY}"
    },
    "auraDsClientSecret": {
      "value": "${AURA_DS_CLIENT_SECRET}"
    },
    "auraDsClientId": {
      "value": "${AURA_DS_CLIENT_ID}"
    },
    "adminApiKey": {
      "value": "${ADMIN_API_KEY}"
    },
    "azureDocumentIntelligenceEndpoint": {
      "value": "${AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT}"
    },
    "azureContentUnderstandingEndpoint": {
      "value": "${AZURE_CONTENT_UNDERSTANDING_ENDPOINT}"
    },
    "azureTranslatorEndpoint": {
      "value": "${AZURE_TRANSLATOR_ENDPOINT}"
    },
    "azureTranslatorRegion": {
      "value": "${AZURE_TRANSLATOR_REGION=swedencentral}"
    },
    "enableSpeechTranslation": {
      "value": "${ENABLE_SPEECH_TRANSLATION=false}"
    },
    "skipRoleAssignments": {
      "value": "${SKIP_ROLE_ASSIGNMENTS=false}"
    },
    "enableAuth": {
      "value": "${ENABLE_AUTH=false}"
    },
    "authClientId": {
      "value": "${AUTH_CLIENT_ID}"
    },
    "authClientSecret": {
      "value": "${AUTH_CLIENT_SECRET}"
    },
    "enableB2C": {
      "value": "${ENABLE_B2C=false}"
    },
    "b2cTenantName": {
      "value": "${B2C_TENANT_NAME}"
    },
    "b2cTenantId": {
      "value": "${B2C_TENANT_ID}"
    },
    "b2cClientId": {
      "value": "${B2C_CLIENT_ID}"
    },
    "b2cClientSecret": {
      "value": "${B2C_CLIENT_SECRET}"
    },
    "b2bCustomDomain": {
      "value": "${B2B_CUSTOM_DOMAIN}"
    },
    "b2cCustomDomain": {
      "value": "${B2C_CUSTOM_DOMAIN}"
    },
    "resourceGroupName": {
      "value": "${RESOURCE_GROUP_NAME=rg-graphrag-feature}"
    },
    "containerRegistryName": {
      "value": "${CONTAINER_REGISTRY_NAME=graphragacr12153}"
    },
    "containerAppsEnvironmentName": {
      "value": "${CONTAINER_APPS_ENVIRONMENT_NAME=graphrag-env}"
    },
    "storageAccountName": {
      "value": "${STORAGE_ACCOUNT_NAME=neo4jstorage21224}"
    },
    "azureOpenAiEndpoint": {
      "value": "${AZURE_OPENAI_ENDPOINT=https://graphrag-openai-8476.openai.azure.com/}"
    },
    "azureOpenAiResourceName": {
      "value": "${AZURE_OPENAI_RESOURCE_NAME=graphrag-openai-8476}"
    },
    "documentIntelligenceName": {
      "value": "${DOCUMENT_INTELLIGENCE_NAME=doc-intel-graphrag}"
    },
    "neo4jUri": {
      "value": "${NEO4J_URI=neo4j+s://501bc8d1.databases.neo4j.io}"
    },
    "neo4jUsername": {
      "value": "${NEO4J_USERNAME=neo4j}"
    },
    "neo4jDatabase": {
      "value": "${NEO4J_DATABASE=neo4j}"
    },
    "groupIdOverride": {
      "value": "${GROUP_ID_OVERRIDE}"
    }
  }
}
